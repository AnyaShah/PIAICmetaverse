// They think of something you could store in a JavaScript Object. Write a program that 
// creates Objects containing these items.

const bio={
      name:"Anya",
      qualify:"Intermediate",
      city:"karachi"
}
let {name,qualify,city} =bio;
console.log(`${name} \n ${qualify} \n ${city}`)
console.log(bio)